import React from 'react';

import StyledLoadingMessage from './StyledLoadingMessage';

const LoadingMessage = ({ text }) => <StyledLoadingMessage>{ text }</StyledLoadingMessage>

export default LoadingMessage;